services:
  - type: web
    name: louie-slack-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 integrations/slack_bot_server.py
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11

